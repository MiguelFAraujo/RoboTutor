from django.apps import AppConfig


class CoreConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core'

    def ready(self):
        # Mantido enxuto para evitar queries no banco durante a inicializacao do app.
        return
